# 🔍 Text Analyzer (Regex Project)

This is a small Python project created to practice **regular expressions** and basic text processing.

The main goal of this project is to learn how to extract structured data from raw text using `re`, and to build a clean, modular project structure that can be extended later.

---

## 📌 Features

- Extract email addresses
- Extract phone numbers with normalization and validation
- Extract dates in different formats
- Extract URLs
- File-based text analysis

---

### 🎯 Learning Goals

- Practice regular expressions (`re`)
- Work with files using `pathlib`
- Learn how to normalize and validate extracted data
- Structure a Python project in a clean and readable way